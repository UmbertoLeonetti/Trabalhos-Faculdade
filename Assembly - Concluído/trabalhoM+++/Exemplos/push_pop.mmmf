MOV 00,E;   //#00
SOMANDO:
	MOV 98,A;
	MOV A,#E;  // MOVE O VALOR DO ACUMULADOR PARA O ENDEREÇO INDICADO "APONTADO" POR B

	MOV E,A;   
	ADD 01,E;  //SOME 01 EM B;

	MOV E,A;
	SUB 20,A;

	JMPZ FIM;

	JMP SOMANDO;

FIM:	JMP FIM;